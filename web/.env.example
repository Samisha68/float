# Public: also set in the Privy dashboard. Never put the app secret in a VITE_ variable.
VITE_PRIVY_APP_ID=
# Server-only credentials. Use the same app as the public ID.
PRIVY_APP_ID=
PRIVY_APP_SECRET=
