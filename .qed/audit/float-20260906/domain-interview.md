# Domain intent interview

Answer only intent questions; source locations and code facts are already captured in `domain-dossier.json`. Record `confirm`, `reject`, or `bug` plus rationale in `domain-interview.json`.

## `src_0af9d98c9bafb84e`

Does `src_0af9d98c9bafb84e` move token_or_lamports_pending from pending:source_account to pending:destination_account in `approve_and_disburse`, and is pending:user the intended amount?

- Decision: pending
- Rationale:

## `src_d8b778c994ad8cb4`

Does `src_d8b778c994ad8cb4` move token_or_lamports_pending from pending:source_account to pending:destination_account in `repay_advance`, and is pending:user the intended amount?

- Decision: pending
- Rationale:

## `src_1630c5f4361e231e`

Is `src_1630c5f4361e231e` (amount) measured in token_amount at scale pending:user, with unknown rounding?

- Decision: pending
- Rationale:

## `src_5104fc12d7f0e029`

Is `src_5104fc12d7f0e029` (fee_bps) measured in basis_points at scale pending:user, with unknown rounding?

- Decision: pending
- Rationale:

## `src_5558d251200e36fa`

Is `src_5558d251200e36fa` (fee_bps) measured in basis_points at scale pending:user, with unknown rounding?

- Decision: pending
- Rationale:

## `src_55a93455daaf6f75`

Is `src_55a93455daaf6f75` (MAX_FEE_BPS) measured in basis_points at scale pending:user, with unknown rounding?

- Decision: pending
- Rationale:

## `src_589cad380eac67ad`

Is `src_589cad380eac67ad` (amount) measured in token_amount at scale pending:user, with unknown rounding?

- Decision: pending
- Rationale:

## `src_89e1d194179954ae`

Is `src_89e1d194179954ae` (amount) measured in token_amount at scale pending:user, with unknown rounding?

- Decision: pending
- Rationale:

## `src_9f5cec7fe54c94c9`

Is `src_9f5cec7fe54c94c9` (amount) measured in token_amount at scale pending:user, with unknown rounding?

- Decision: pending
- Rationale:

## `src_a142794e08ba2451`

Is `src_a142794e08ba2451` (fee) measured in token_amount at scale pending:user, with unknown rounding?

- Decision: pending
- Rationale:

## `src_dc1a2fc9fe511a45`

Is `src_dc1a2fc9fe511a45` (BPS_DENOMINATOR) measured in basis_points at scale pending:user, with unknown rounding?

- Decision: pending
- Rationale:

